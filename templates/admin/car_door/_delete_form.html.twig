<form method="post" action="{{ path('app_admin_car_door_delete', {'id': car_door.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_door.id) }}">
    <button class="btn">Delete</button>
</form>
