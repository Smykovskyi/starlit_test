{% extends 'base.html.twig' %}

{% block title %}CarDoor{% endblock %}

{% block body %}
    <h1>CarDoor</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_door.id }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ car_door.model }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ car_door.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_car_door_index') }}">back to list</a>

    <a href="{{ path('app_admin_car_door_edit', {'id': car_door.id}) }}">edit</a>

    {{ include('admin/car_door/_delete_form.html.twig') }}
{% endblock %}
